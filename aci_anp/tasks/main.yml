---
- name: Add a new ANP to Tenant
  aci_ap:
   host: apic1
   username: "{{ aci_username }}"
   password: "{{ aci_password }}"
   validate_certs: no
   tenant: "{{ item.tenant }}"
   ap: "{{ item.name }}"
   description: "{{ item.description }}"
   state: present
  with_items: "{{ anps }}"
